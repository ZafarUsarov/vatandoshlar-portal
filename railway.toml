[build]
builder = "RAILPACK"
buildCommand = "npm run build"

[deploy]
preDeployCommand = "npm run db:migrate"
startCommand = "npm run geoip:prepare && npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
