import ResetPasswordForm from "@/components/auth/ResetPasswordForm";
import { Link } from "@/i18n/navigation";
export const metadata = { title: "Reset password", robots: { index: false, follow: false } };
export default async function Page({ params, searchParams }: { params: Promise<{ locale: string }>; searchParams: Promise<{ token?: string }> }) { const [{ locale: raw }, q] = await Promise.all([params, searchParams]); const locale = raw === "de" ? "de" : "uz"; const token = typeof q.token === "string" ? q.token : ""; return <main className="min-h-screen bg-slate-50 px-4 py-12 dark:bg-slate-950"><div className="mx-auto max-w-md rounded-[2rem] border border-slate-200 bg-white p-6 shadow-xl shadow-slate-900/5 dark:border-slate-800 dark:bg-slate-900 sm:p-8"><h1 className="text-3xl font-black text-slate-950 dark:text-white">{locale === "de" ? "Neues Passwort" : "Yangi parol"}</h1><div className="mt-8">{token ? <ResetPasswordForm locale={locale} token={token}/> : <p role="alert" className="text-sm text-red-600">{locale === "de" ? "Der Link ist ungültig." : "Havola yaroqsiz."}</p>}</div><Link href="/id/login" className="mt-6 block text-center text-sm font-semibold text-emerald-700 dark:text-emerald-300">{locale === "de" ? "Zur Anmeldung" : "Kirishga qaytish"}</Link></div></main>; }
