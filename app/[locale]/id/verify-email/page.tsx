import { verifyEmailWithToken } from "@/lib/auth/public-auth-token-repository";
import { Link } from "@/i18n/navigation";
export const metadata = { title: "Verify email", robots: { index: false, follow: false } };
export default async function Page({ params, searchParams }: { params: Promise<{ locale: string }>; searchParams: Promise<{ token?: string }> }) { const [{ locale: raw }, q] = await Promise.all([params, searchParams]); const locale = raw === "de" ? "de" : "uz"; const token = typeof q.token === "string" ? q.token : ""; let ok = false; if (token) { try { ok = await verifyEmailWithToken(token); } catch (error) { console.error("Email verification failed", error); } } return <main className="min-h-screen bg-slate-50 px-4 py-12 dark:bg-slate-950"><div className="mx-auto max-w-md rounded-[2rem] border border-slate-200 bg-white p-6 shadow-xl shadow-slate-900/5 dark:border-slate-800 dark:bg-slate-900 sm:p-8"><h1 className="text-3xl font-black text-slate-950 dark:text-white">{ok ? (locale === "de" ? "E-Mail bestätigt" : "E-mail tasdiqlandi") : (locale === "de" ? "Bestätigung nicht möglich" : "Tasdiqlash amalga oshmadi")}</h1><p className="mt-4 text-sm text-slate-600 dark:text-slate-400">{ok ? (locale === "de" ? "Sie können sich jetzt anmelden." : "Endi tizimga kirishingiz mumkin.") : (locale === "de" ? "Der Link ist ungültig, abgelaufen oder wurde bereits verwendet." : "Havola yaroqsiz, muddati tugagan yoki avval ishlatilgan.")}</p><Link href={ok ? "/id/login" : "/id/resend-verification"} className="mt-6 inline-flex min-h-12 w-full items-center justify-center rounded-2xl bg-emerald-600 px-5 text-sm font-bold text-white">{ok ? (locale === "de" ? "Anmelden" : "Kirish") : (locale === "de" ? "Neue E-Mail anfordern" : "Yangi xat so‘rash")}</Link></div></main>; }
