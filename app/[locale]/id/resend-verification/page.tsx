import AuthEmailRequestForm from "@/components/auth/AuthEmailRequestForm";
import { Link } from "@/i18n/navigation";
export const metadata = { title: "Email verification", robots: { index: false, follow: false } };
export default async function Page({ params }: { params: Promise<{ locale: string }> }) { const { locale: raw } = await params; const locale = raw === "de" ? "de" : "uz"; return <main className="min-h-screen bg-slate-50 px-4 py-12 dark:bg-slate-950"><div className="mx-auto max-w-md rounded-[2rem] border border-slate-200 bg-white p-6 shadow-xl shadow-slate-900/5 dark:border-slate-800 dark:bg-slate-900 sm:p-8"><h1 className="text-3xl font-black text-slate-950 dark:text-white">{locale === "de" ? "Bestätigungs-E-Mail erneut senden" : "Tasdiqlash xatini qayta yuborish"}</h1><p className="mt-3 mb-8 text-sm text-slate-600 dark:text-slate-400">{locale === "de" ? "Geben Sie die E-Mail-Adresse Ihres Kontos ein." : "Hisobingiz e-mail manzilini kiriting."}</p><AuthEmailRequestForm locale={locale} mode="resend"/><Link href="/id/login" className="mt-6 block text-center text-sm font-semibold text-emerald-700 dark:text-emerald-300">{locale === "de" ? "Zur Anmeldung" : "Kirishga qaytish"}</Link></div></main>; }
